const config = {
  module: {
    name: "vtta-core",
    label: "VTTA Core",
  },
  messaging: {
    core: {
      query: "vtta-core.query",
      response: "vtta-core.available",
    },
  },
};

export default config;
